<?xml version="1.0" encoding="utf-8"?>
<!--
  Single news row inside the ListView.
  Height: ~62dp (meta row ~16dp + headline ~38dp + vertical padding ~8dp).
  No horizontal padding — the ListView itself carries 13dp start/end.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="9dp"
    android:paddingBottom="9dp">

    <!-- Meta row: favicon | source tag | time -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="16dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="4dp">

        <ImageView
            android:id="@+id/favicon"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:scaleType="fitCenter"
            android:layout_marginEnd="5dp" />

        <TextView
            android:id="@+id/source_tag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Source"
            android:textSize="7sp"
            android:fontFamily="monospace"
            android:textAllCaps="true"
            android:letterSpacing="0.05"
            android:paddingStart="5dp"
            android:paddingEnd="5dp"
            android:paddingTop="1dp"
            android:paddingBottom="1dp"
            android:background="@drawable/tag_border_bg" />

        <TextView
            android:id="@+id/time_ago"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="—"
            android:textColor="@color/text_faint"
            android:textSize="8sp"
            android:fontFamily="monospace"
            android:gravity="end" />

    </LinearLayout>

    <!-- Headline -->
    <TextView
        android:id="@+id/headline"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text=""
        android:textColor="@color/text_secondary"
        android:textSize="12sp"
        android:lineSpacingMultiplier="1.5"
        android:maxLines="2"
        android:ellipsize="end" />

</LinearLayout>
